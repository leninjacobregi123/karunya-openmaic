# Env file for docker-compose.remote.yml. Copy to `.env` (compose auto-loads
# it) and change every value below before exposing this to anyone but you.
#
#   cp .env.remote.example .env

# --- Secrets (CHANGE THESE) ---------------------------------------------------
POSTGRES_PASSWORD=change-me-postgres
MINIO_ROOT_PASSWORD=change-me-minio
SESSION_SECRET=change-me-to-a-long-random-string

# --- Dev accounts --------------------------------------------------------------
# true  = seed admin@karunya.edu/teacher123 + student1/student2 (weak, well-known
#         passwords - fine for a first smoke test, NOT for anything real).
# false = skip seeding; you'll need another way to create accounts (real AD/LDAP
#         wiring is a documented follow-up in docs/karunya-architecture.md).
MAIC_SEED_DEV_USERS=true

# --- Models ----------------------------------------------------------------
# The LLM is NOT containerized in docker-compose.remote.yml - it expects an
# OpenAI-compatible server (e.g. vLLM) reachable at :8001.
# Format: "lemonade:<exact model name your vLLM instance serves>" - e.g.
# lemonade:Qwen/Qwen2.5-32B-Instruct. No default: this MUST match whatever
# you actually started vLLM with (check its startup log / `--model` flag, or
# GET http://<vllm-host>:8001/v1/models).
MAIC_DEFAULT_MODEL=lemonade:REPLACE_WITH_YOUR_VLLM_MODEL_NAME

# Only needed if vLLM runs on a DIFFERENT machine than this one (leave unset
# if vLLM runs directly on this host - it then defaults to the host itself).
# Full base URL including scheme and /v1 path, e.g.:
# MAIC_VLLM_BASE_URL=http://192.168.6.249:8001/v1
#MAIC_VLLM_BASE_URL=

# --- Ports (host-side; change if already taken) ------------------------------
MAIC_APP_PORT=3000
MAIC_MINIO_CONSOLE_PORT=9001
